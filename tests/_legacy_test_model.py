#!/usr/bin/env python3
"""
Legacy, verbose test harness preserved for manual runs.

Move to `scripts/` or run directly via `python tests/_legacy_test_model.py`.
"""

# The original verbose test script is preserved below for manual debugging.

import torch
from transformers import AutoTokenizer


def main():
    print("This legacy test harness was moved to legacy/ for manual runs.")


if __name__ == "__main__":
    main()
