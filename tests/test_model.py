"""This manual model tester has been moved to `legacy/test_model_legacy.py`.

To run local manual checks that load large model files, put them under
`legacy/downloaded_model/` and run:

    python legacy/test_model_legacy.py

This keeps the repository root clean and prevents accidental commits of
large artifacts.
"""

print("This script has been moved to legacy/test_model_legacy.py")
