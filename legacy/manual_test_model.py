"""Manual model tester (moved from repo root).

Place local model files under `legacy/downloaded_model/` and run this script
manually to avoid interfering with pytest collection.
"""

print("Run manual model tests from legacy/manual_test_model.py")
