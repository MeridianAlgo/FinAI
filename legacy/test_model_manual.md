This file explains how to run the manual `test_model.py` script safely.

If you need to load local model weights for testing, place them under
`legacy/downloaded_model/` and run `python legacy/test_model_legacy.py`.
