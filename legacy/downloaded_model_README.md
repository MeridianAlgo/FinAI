This folder previously contained `downloaded_model/` artifacts. Move any large downloaded model artifacts here or keep them in external storage.

Prefer using streaming and keep large models out of the repository.
